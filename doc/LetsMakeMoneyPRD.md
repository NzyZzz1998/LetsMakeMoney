# LetsMakeMoney 赚钱模拟器 PRD

**版本**: v0.1 (Beta)  
**日期**: 2026-06-24  
**状态**: 草稿

---

## 1. 项目概述

### 1.1 产品定位

**LetsMakeMoney（赚钱模拟器）** 是一款桌面宠物软件，通过可爱的小动物形象陪伴用户工作，并基于用户设定的薪资参数实时计算"这一刻已经赚了多少钱"。

产品兼顾两种价值：
- **情感陪伴**：扁平卡通风格的小动物在桌面上敲键盘、休息、互动，提供治愈感
- **实用反馈**：严肃真实的薪资计算逻辑，让用户时刻感知时间价值

### 1.2 目标用户

- 长期在电脑前工作的上班族、自由职业者
- 希望通过趣味化方式感知时间价值的人群
- 喜欢桌面宠物/治愈系软件的用户

### 1.3 核心价值

> 用一只努力敲键盘的小动物告诉你："你正在赚钱"

---

## 2. 平台与版本规划

### 2.1 当前版本（v0.1 Beta）

| 项目 | 说明 |
|------|------|
| 目标平台 | Windows |
| 技术栈 | Godot 引擎（GDScript） |
| 选型理由 | 轻量、原生支持 Win/Mac/iOS/Android 导出，适合 2D 动画 |
| 版本定位 | Beta 阶段，功能逐步完善中 |

### 2.2 后续规划

| 版本 | 平台 | 说明 |
|------|------|------|
| v0.x | Windows | Beta 阶段，逐步完善功能至稳定 |
| v1.0 | Windows | 首个正式版，功能稳定 |
| v2.0 | 新增 macOS 支持 | 跨平台起步 |
| v3.0 | 新增 iOS / Android 支持 | 移动端覆盖 |

**技术选型约束**：v0.1 的架构设计必须为后续跨平台移植预留接口，避免硬编码 Windows-only 逻辑（托盘、窗口管理部分需封装为抽象层）。

---

## 3. 功能需求

### 3.1 核心功能

| 编号 | 功能 | 说明 |
|------|------|------|
| F1 | 实时薪资计算 | 基于月薪/工作天数/工作时数/上下班时间，每秒更新"今日已赚" |
| F2 | 桌面宠物显示 | 扁平卡通风格小动物常驻桌面 |
| F3 | 角色动画系统 | 工作/休息/待机/悬停/点击反馈等状态动画 |
| F4 | 薪资面板 | 角色旁独立小面板显示简洁数字，悬停展开详情 |
| F5 | 用户交互 | 单击/双击/长按/拖拽/右键菜单 |
| F6 | 设置系统 | 薪资参数、角色、显示、面板项配置 |
| F7 | 托盘驻留 | 关闭窗口缩到托盘，右键菜单操作 |
| F8 | 首次启动向导 | 无薪资配置时弹出引导窗口，同时角色已在桌面待机 |

### 3.2 交互需求

| 触发方式 | 反应 |
|----------|------|
| 单击角色 | 轻量反馈动画（如摸头、跳跃） |
| 双击角色 | 触发特殊动画（如欢呼、撒花） |
| 长按角色（≥ 500ms） | 呆住/疑惑动画 |
| 拖拽角色 | 移动窗口位置 |
| 右键角色 | 弹出菜单（见 3.2.1） |
| 鼠标悬停面板（≥ 300ms） | 展开详情面板 |
| 鼠标离开面板（≥ 500ms） | 收起详情面板 |
| 点击托盘图标 | 显示/隐藏角色 |
| 右键托盘图标 | 弹出菜单（含显示/隐藏/设置/切换角色/退出） |

面板展开/收起采用平滑过渡动画（ease-in-out, 200ms）。面板始终显示在角色旁边，但智能调整方位：当角色贴近屏幕右边缘时面板出现在角色左侧，贴下边缘时出现在上方，避免面板溢出屏幕。

#### 3.2.1 右键菜单结构

**右键角色菜单：**

```
├── 设置
├── 切换角色
│     ├── 🐱 小猫
│     ├── 🐶 小狗
│     └── 🐹 仓鼠
├── 窗口模式
│     ├── 置顶悬浮
│     └── 融入桌面
├── ──────────
├── 关于 LetsMakeMoney
└── 退出
```

**右键托盘菜单：** 在上述基础上增加"显示/隐藏"（位于菜单顶部）。

### 3.3 薪资计算规则

**输入参数**（用户设定）：
- 月薪 `monthly_salary`
- 休息模式 `rest_mode`：`single`（单休，仅周日休息）或 `double`（双休，周六日休息）
- 每日工作小时数 `work_hours_per_day`
- 上班时间 `work_start_time`（如 09:00）
- 下班时间 `work_end_time`（如 18:00）

**每月工作天数**：由系统根据休息模式 + 当前月份日历自动计算，无需用户手动输入。

| 模式 | 工作天数 | 示例（2026年6月） |
|------|---------|-------------------|
| 双休 | 当月周一至周五的天数 | 22 天 |
| 单休 | 当月周一至周六的天数 | 26 天 |

每月首日自动重新计算当月工作天数，薪资引擎使用新值更新 `rate_per_second` 和 `hourly_rate`。

**派生值**：
- 当月工作天数 `work_days = calc_work_days(year, month, rest_mode)` — 根据日历经月份自动计算
- 每秒收入 `rate_per_second = monthly_salary / (work_days × work_hours_per_day × 3600)`
- 时薪 `hourly_rate = monthly_salary / (work_days × work_hours_per_day)`
- 每月首日自动重算 `work_days`，连带更新 `rate_per_second` 和 `hourly_rate`

**实时计算**：
- **工作时间判定**：当前时间在 `work_start_time` 与 `work_end_time` 之间 → 角色处于 WORKING 状态，数字增长
- **今日已赚**：
  - 上班前（当前时间 < `work_start_time`）：0
  - 工作中（`work_start_time` ≤ 当前时间 ≤ `work_end_time`）：`(当前时间 - work_start_time) × rate_per_second`
  - 下班后（当前时间 > `work_end_time`）：`work_hours_per_day × 3600 × rate_per_second`（封顶为全天工作总收入）
- **本月累计**：`monthly_salary × (今日日期 / 当月天数)`
- **工作进度**：`今日工作已过秒数 / (work_hours_per_day × 3600)`，下班后为 100%

**非工作时间**：
- 角色切换到 RESTING 状态（睡觉/打盹动画）
- "今日已赚"在上班前显示 0，下班后保持全天工作总收入值（不再增长）

---

## 4. 角色与视觉

### 4.1 视觉风格

- **风格**：扁平卡通矢量
- **v0.1 占位方案**：使用免费像素风素材（如 [itch.io Cat 2D Pixel Art](https://xzany.itch.io/cat-2d-pixel-art)）作为占位素材进行原型开发。v1.0 正式版替换为扁平卡通矢量素材。
- **线条**：清晰、柔和
- **色彩**：鲜明温暖
- **尺寸**：原生像素 64×64 px（v0.1 占位素材），显示时缩放到约 120×120 px，用户可调（50% - 200%）

### 4.2 角色规划

**v0.1 初始角色（2-3 个）**：
- 小猫 🐱
- 小狗 🐶
- 仓鼠 🐹（可选，视开发进度）

**架构要求**：角色通过 `PetResource` (.tres) 资源文件定义，新增角色只需在 `pets/` 目录添加资源文件，无需修改核心代码。

### 4.3 动画状态机

状态切换由 Pet Manager 根据薪资引擎的工作时间判定和鼠标事件驱动。

```
IDLE       待机/呼吸动画（鼠标未靠近）
WORKING    疯狂敲键盘（工作时间内）
RESTING    睡觉/打盹（非工作时间）
HOVER      看向鼠标（鼠标悬停时）
CLICKED    被点击反馈动画（单击/双击/长按各有变体）
```

#### 4.3.1 动画规格

| 状态 | 帧数 | 循环/单次 | 周期 | 说明 |
|------|------|-----------|------|------|
| IDLE | 4 | 循环 | 2000ms | 站立呼吸/微微摆动，鼠标未靠近时默认播放 |
| WORKING | 4 | 循环 | 800ms | 快速敲键盘动作，工作时间播放 |
| RESTING | 2 | 循环 | 3000ms | 闭眼起伏呼吸/打盹，非工作时间播放 |
| HOVER | 1 | 静态 | — | 切换到"看向鼠标"的单帧姿态，鼠标离开后回到 IDLE |
| CLICKED（单击）| 3 | 单次 | 600ms | 被摸头/弹跳反馈，播放完后回到先前状态 |
| CLICKED（双击）| 3 | 单次 | 800ms | 欢呼/撒花变体，播放完后回到先前状态 |
| CLICKED（长按）| 2 | 循环 | 1000ms | 呆住/疑惑表情，松开后回到先前状态 |

**帧来源标注**：v0.1 占位素材的 IDLE / RESTING / CLICKED 帧来自免费像素素材包；WORKING（敲键盘）和 HOVER 帧需手动调整或创建。

#### 4.3.2 Godot 动画实现

使用 AnimatedSprite2D + SpriteFrames 资源。每个角色一个 `.tres` SpriteFrames 文件，包含上述所有动画。PetResource 存储对 SpriteFrames 资源的引用。

```
pets/
  cat/
    cat_sprite_frames.tres   # SpriteFrames 资源
    cat_resource.tres         # PetResource（引用 sprite_frames）
  dog/
    dog_sprite_frames.tres
    dog_resource.tres
  hamster/
    hamster_sprite_frames.tres
    hamster_resource.tres
```

---

## 5. UI 设计

### 5.1 窗口模式

用户可在设置中切换：

| 模式 | 行为 |
|------|------|
| 置顶悬浮 | 始终在所有窗口之上，不挡主内容，类似悬浮球 |
| 融入桌面 | 像桌面小部件，点击桌面空白处可见 |

默认值：置顶悬浮。

**两种模式统一行为**：

| 行为 | 说明 |
|------|------|
| 任务栏显示 | 不显示在任务栏 |
| Alt+Tab 列表 | 不出现 |
| 系统托盘 | 始终显示托盘图标 |
| 交互 | 两种模式下均可正常拖拽、点击、右键，无差异 |

### 5.2 薪资面板

**简洁模式（常态）**：
```
┌──────────────┐
│  💰 ¥12.34   │
└──────────────┘
```
半透明背景，角色旁边显示今日已赚。

**展开模式（鼠标悬停）**：
```
┌────────────────────────┐
│  今日已赚    ¥12.34     │
│  本月累计    ¥370.20    │
│  时薪        ¥46.88     │
│  ───────────────────    │
│  工作进度    2h/8h  ██░░ │
│  状态        🐱 努力工作中 │
└────────────────────────┘
```

**隐私控制**：展开模式的每一项内容支持用户在设置中开启/关闭，默认全显示。用户可隐藏敏感项（如本月累计、时薪）。

**交互时序**：
- 悬停面板 300ms → 触发平滑展开（ease-in-out，200ms 动画）
- 鼠标离开面板 500ms → 触发平滑收起（ease-in-out，200ms 动画）
- 期间鼠标重新进入 → 取消收起，保持展开

**面板定位**：默认在角色右侧。若角色贴屏幕右边缘（右侧空间不足），面板自动出现于左侧；若贴底部则上方弹出。每次展开时动态计算最佳位置。

### 5.3 位置与记忆

- 首次启动默认位置：屏幕右下角
- 用户拖拽后，退出时保存屏幕坐标
- 下次启动还原上次位置
- 若保存的位置在当前屏幕外（多显示器切换）， fallback 到右下角

### 5.4 透明度与大小

| 项 | 范围 | 默认 |
|----|------|------|
| 角色透明度 | 20% - 100% | 100% |
| 角色缩放 | 50% - 200% | 100% |

---

## 6. 设置项汇总

设置入口：右键角色 / 右键托盘 → "设置"

### 6.1 对话框布局

采用**标签页式布局**，分 5 个标签页：

```
┌─────────────────────────────────┐
│  设置                      ✕   │
├─────────────────────────────────┤
│ [薪资] [角色] [显示] [面板] [通用]  │
├─────────────────────────────────┤
│                                 │
│  当前标签页内容...               │
│                                 │
├─────────────────────────────────┤
│              [取消]    [保存]    │
└─────────────────────────────────┘
```

**保存行为**：点击"保存"按钮才会将修改写入 Config 并应用。点击"取消"或关闭窗口则放弃所有修改。透明度/缩放在保存前不预览（简化 v0.1 实现）。

### 6.2 配置项明细

| 分类 | 选项 | 控件类型 | 说明 |
|------|------|----------|------|
| **薪资** | 月薪 | 数值输入框 | |
| | 休息模式 | 下拉选择 | 双休（默认）/ 单休 |
| | 每日工作小时数 | 数值输入框 | 默认 8 |
| | 上班时间 | 时间选择器 | 默认 09:00 |
| | 下班时间 | 时间选择器 | 默认 18:00 |
| **角色** | 切换角色 | 列表选择 | 从 `pets/` 目录动态加载 |
| | 角色缩放 | 滑块 | 50%-200%，默认 100% |
| **显示** | 透明度 | 滑块 | 20%-100%，默认 100% |
| | 窗口模式 | 下拉选择 | 置顶（默认）/ 融入桌面 |
| **面板** | 展开详情项开关 | 复选框×5 | 今日已赚/本月累计/时薪/工作进度/状态，默认全开 |
| **通用** | 开机自启 | 开关（禁用态） | v0.1 预留 |
| | 语言 | 下拉选择 | 当前仅中文 |

---

## 7. 非功能性需求

### 7.1 性能

- 空闲状态下 CPU 占用 < 1%
- 动画运行时 CPU 占用 < 5%
- 内存占用 < 100 MB

### 7.2 静音

- v0.1 不包含任何音效和背景音乐
- 纯视觉桌面宠物，不打扰用户

### 7.3 单角色

- v0.1 桌面上同时只有一只小动物
- 架构上不阻止未来扩展为多角色

### 7.4 数据持久化

配置文件保存路径通过 PlatformInterface 获取（跨平台抽象，见 8.3）：

- Windows: `%APPDATA%/LetsMakeMoney/config.json`
- macOS（未来）: `~/Library/Application Support/LetsMakeMoney/config.json`
- iOS/Android（未来）: 应用沙箱对应目录

保存内容：
- 薪资参数（月薪、休息模式、工作时数、上下班时间）
- 选中的角色 ID
- 窗口位置（x, y）
- 窗口模式（置顶 / 嵌入）
- 透明度、缩放
- 面板展开项的开关状态

---

## 8. 架构设计

### 8.1 模块结构

```
┌─────────────────────────────────────────────────────┐
│                   Main App (Node)                    │
│          桌面窗口管理、置顶/嵌入模式、托盘系统            │
├─────────┬─────────┬──────────┬──────────┬────────────┤
│ Salary  │ Pet     │ Panel    │ Settings │ DragResize │
│ Engine  │ Manager │  System  │ System   │  System    │
│ (Auto)  │ (Auto)  │ (Auto)   │ (Auto)   │  (Auto)    │
├─────────┴─────────┴──────────┴──────────┴────────────┤
│                    Config (Auto)                      │
│          用户配置持久化（透明度/大小/位置/薪资）            │
└─────────────────────────────────────────────────────┘
```

### 8.2 模块职责

#### Salary Engine（薪资引擎）— Autoload 单例

- 接收薪资参数，计算每秒收入、时薪
- 判定当前是否为工作时间
- 根据休息模式和当前月份日历自动计算当月工作天数
- 每月首日自动刷新工作天数和时薪
- 提供 `get_earnings_today()`、`get_earnings_this_month()`、`get_rate_per_second()`、`is_working_hours()`、`get_work_days_this_month()` 接口

#### Pet Manager（角色管理器）— Autoload 单例

- 加载 `pets/` 目录下的所有 `PetResource`
- 管理角色状态机（IDLE / WORKING / RESTING / HOVER / CLICKED）
- 根据薪资引擎判定切换 WORKING / RESTING
- 提供切换角色接口

#### Panel System（面板系统）— Autoload 单例

- 简洁模式：显示今日已赚数字
- 展开模式：根据用户配置显示对应详情项
- 鼠标悬停触发展开

#### Settings System（设置系统）— 独立 Scene

- 配置项 UI（见表 6）
- 保存时写入 Config 单例
- 实时应用变更（如调透明度立即生效）

#### Drag & Resize System — Autoload 单例

- 拖拽角色区域移动窗口
- 右键菜单（设置/切换角色/切换模式/退出）
- 位置记忆与还原

#### Config — Autoload 单例

- 加载/保存 `config.json`
- 提供配置读写接口
- 配置变更通知（观察者模式）

### 8.3 跨平台抽象层

Windows 相关逻辑（托盘、窗口置顶/嵌入桌面、开机自启）封装在 `platform/` 目录下，通过接口抽象：

```
platform/
  PlatformInterface.gd    # 抽象接口
  WindowsPlatform.gd      # Windows 实现
  # 未来：MacPlatform.gd、LinuxPlatform.gd
```

v0.1 只实现 WindowsPlatform，但所有调用通过 PlatformInterface，为后续移植铺路。

---

## 9. 首次启动流程

首次启动时，Config 加载失败（无 config.json）→ 触发引导向导。角色同时在桌面上以 IDLE 状态活动。

### 9.1 向导步骤

引导向导为独立的模态窗口，分 4 步，底部有"上一步 / 下一步"按钮：

```
Step 1: 欢迎页
  - 中央展示当前默认角色的大尺寸 IDLE 动画
  - 标题："欢迎来到 LetsMakeMoney"
  - 副标题："让一只可爱的小动物陪你一起赚钱吧！"
  - [下一步]

Step 2: 薪资设置
  - 月薪  [________] 元
  - 休息模式  [双休 ▾] / [单休 ▾]
  - 每日工作小时数 [___] 小时（默认 8）
  - 上班时间  [09:00]
  - 下班时间  [18:00]
  - [上一步] [下一步]

Step 3: 选角色
  - 展示可用角色列表（2-3 个缩略图/名称）
  - 点击选中，选中的高亮显示
  - 选中的角色实时在桌面上替换显示
  - [上一步] [下一步]

Step 4: 完成
  - 确认信息摘要："月薪 ¥XX,XXX | 双休/单休 每天X小时 | XX:XX-XX:XX | 🐱 小猫"
  - [上一步] [开始赚钱！]
  - 点击"开始赚钱" → 保存配置 → 引导窗口关闭 → 角色进入正常状态
```

---

## 10. 开发里程碑（建议）

| 阶段 | 内容 | 产出 |
|------|------|------|
| M1 | 薪资引擎 + Config | 可独立测试的计算模块 |
| M2 | 角色管理 + 状态机 + 初始角色素材 | 桌面上能看到会动的小动物 |
| M3 | 面板系统 + 交互（拖拽/右键/悬停） | 完整桌面宠物雏形 |
| M4 | 设置系统 + 托盘 + 首次启动向导 | 可用产品 |
| M5 | 打包发布 + 跨平台抽象层验证 | v0.1 Beta 发布 |

---

## 11. 范围外（v0.1 不做）

- 音效和背景音乐
- 多角色同屏
- macOS / iOS / Android 支持（v2.0+）
- 开机自启（v0.1 预留接口，后续版本启用）
- 角色自定义/上传
- 多语言（当前仅中文）
- 云同步配置
- 数据统计/报表

---

## 12. 风险与待确认

| 风险 | 说明 | 缓解方案 |
|------|------|----------|
| Godot 窗口嵌入桌面模式 | Windows 下"融入桌面"需穿透鼠标事件，可能不稳定 | 默认置顶模式，融入桌面作为可选项，不稳定时降级 |
| 角色素材 | 需要扁平卡通风格的 2-3 个角色，每个 5 状态动画 | 可先用占位素材，后期替换；或使用开源素材 |
| 托盘系统集成 | Godot 原生不提供托盘，需用 GDExtension 或插件 | 调研 Godot 4.x 的 System Tray 插件可行性 |

---

## 附录 A：术语表

| 术语 | 说明 |
|------|------|
| Autoload | Godot 的全局单例机制，项目启动时自动加载 |
| PetResource | 自定义 Godot Resource 类型，存储单个角色数据 |
| 置顶悬浮 | 窗口始终在所有窗口之上 |
| 融入桌面 | 窗口作为桌面元素，不置顶 |
| 工作时间 | 用户设定的上下班时间区间 |

---

*本文档由 brainstorming 流程生成，下一步将进入 writing-plans 创建详细实施计划。*
