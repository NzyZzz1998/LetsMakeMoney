# LetsMakeMoney v0.4 Beta 验证文档

**拆分日期**: 2026-07-08
**来源文件**: doc/verification/v0.4.md
**说明**: 本文件复制自当前 v0.4 验证文档，用作 v0.4 专属文档入口。正式迁移前，若与 doc/verification/v0.4.md 不一致，请优先人工确认最新复测记录。

---

# LetsMakeMoney v0.4 Beta 手动验证文档

更新时间：2026-07-06

本文档用于记录 v0.4 Beta 的手动验收结果。v0.4 仍处于测试分支阶段，重点验证橘猫 v2 动画、桌宠交互、Panel/右键菜单/设置页暖色 UI、窗口行为、托盘与稳定性。

结果建议填写：
- `通过`
- `失败`
- `需优化`
- `暂缓`
- `未测`

## 1. 基础信息

| 项目 | 填写 |
|------|------|
| 验证日期 | |
| Windows 版本 | |
| Godot 版本 | 4.7 stable |
| 运行方式 | 导出 exe / Godot 编辑器 |
| 项目路径 | `<PROJECT_ROOT>` |
| exe 路径 | `<PROJECT_ROOT>\build\LetsMakeMoney.exe` |
| 配置路径 | `%APPDATA%\LetsMakeMoney\config.json` |
| debug 日志路径 | `%APPDATA%\LetsMakeMoney\debug.log` |
| 验证结论 | 通过 / 阻塞 / 需优化 |

## 2. 验证前准备

### 2.1 关闭旧进程

```powershell
Stop-Process -Name LetsMakeMoney -Force -ErrorAction SilentlyContinue
```

结果：
备注：

### 2.2 可选：备份并重置配置

配置文件通常位于：

```text
%USERPROFILE%\AppData\Roaming\LetsMakeMoney\config.json
```

打开配置目录：

```powershell
explorer "$env:APPDATA\LetsMakeMoney"
```

如需干净验证：

```powershell
Copy-Item "$env:APPDATA\LetsMakeMoney\config.json" "$env:APPDATA\LetsMakeMoney\config.backup.v04.json" -ErrorAction SilentlyContinue
Remove-Item "$env:APPDATA\LetsMakeMoney\config.json" -Force -ErrorAction SilentlyContinue
```

结果：
备注：

### 2.3 自动验证

在项目目录执行：

```powershell
cd <PROJECT_ROOT>
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify_v04.ps1
```

预期：
- 输出包含 `v0.4 verification passed`。
- 不出现 parser/runtime error。
- v0.4 关键节点、配置默认值、动画资源和 UI 尺寸检查通过。

结果：
备注：

## 3. 桌宠启动与窗口

| 编号          | 操作步骤                           | 预期行为                 | 结果  | 备注  |
| ----------- | ------------------------------ | -------------------- | --- | --- |
| V04-MAN-001 | 双击运行 `build\LetsMakeMoney.exe` | 默认进入透明无边框桌宠模式        | 通过  |     |
| V04-MAN-002 | 观察桌面窗口                         | 小猫、Panel 可见，背景不应出现黑块 | 通过  |     |
| V04-MAN-003 | 拖动小猫区域                         | 窗口跟随鼠标移动，速度正常        | 通过  |     |
| V04-MAN-004 | 关闭并重新启动                        | 窗口位置恢复到上次位置          | 通过  |     |
| V04-MAN-005 | 查看任务栏                          | 应保留可找回入口             | 通过  |     |

## 4. 橘猫 v2 动画

| 编号          | 操作步骤           | 预期行为                   | 结果  | 备注                                                                  |
| ----------- | -------------- | ---------------------- | --- | ------------------------------------------------------------------- |
| V04-MAN-010 | 默认静置观察 10 秒    | 橘猫 v2 正常显示，idle 动画自然   | 通过  |                                                                     |
| V04-MAN-011 | 设置为工作中时段后观察    | working 动画显示电脑/金币等工作道具 | 通过  |                                                                     |
| V04-MAN-012 | 设置为休息时段后观察     | resting 动画自然，不出现裁切或错位  | 通过  |                                                                     |
| V04-MAN-013 | 左键单击小猫         | 触发当前基础状态对应的单击延伸动作，视觉可见 | 通过  |                                                                     |
| V04-MAN-014 | 左键双击小猫         | 触发当前基础状态对应的双击延伸动作，视觉可见 | 通过  |                                                                     |
| V04-MAN-015 | 左键长按约 0.7 秒后松开 | 触发长按反馈，松开后回到自动状态       | 通过  | 已修复：长按触发后再移动/拖拽时保留 clicked_hold 反馈，拖拽结束后走正常长按回退路径，不再立即切回 hover/auto |

## 5. Panel 与右键菜单 UI

| 编号          | 操作步骤        | 预期行为                     | 结果  | 备注  |
| ----------- | ----------- | ------------------------ | --- | --- |
| V04-MAN-020 | 观察折叠态 Panel | 不再是黑色玻璃风，呈现金币小票/便签风      | 通过  |     |
| V04-MAN-021 | 鼠标悬停 Panel  | Panel 展开，布局像温暖小票，文字清晰不挤压 | 通过  |     |
| V04-MAN-022 | 鼠标移开 Panel  | Panel 收起，动效轻微自然          | 通过  |     |
| V04-MAN-023 | 右键小猫        | 弹出暖色纸面风格菜单               | 通过  |     |
| V04-MAN-024 | 悬停菜单项       | hover 为淡金币色，文字清晰         | 通过  |     |
| V04-MAN-025 | 打开二级菜单      | 窗口模式/选择宠物等二级菜单可用且风格一致    | 通过  |     |

## 6. Settings UI

| 编号          | 操作步骤            | 预期行为                   | 结果  | 备注  |
| ----------- | --------------- | ---------------------- | --- | --- |
| V04-MAN-030 | 右键小猫，点击“设置”     | 打开紧凑暖色偏好设置面板           | 通过  |     |
| V04-MAN-031 | 查看“工资”页         | 页面无顶部大 header，行式设置紧凑清晰 | 通过  |     |
| V04-MAN-032 | 切换“桌宠”页         | 宠物选择可用，页面不溢出           | 通过  |     |
| V04-MAN-033 | 切换“显示”页         | 透明度、缩放、窗口模式、纯桌宠模式布局正常  | 通过  |     |
| V04-MAN-034 | 拖动透明度/缩放 slider | 数值更新，控件风格统一            | 通过  |     |
| V04-MAN-035 | 展开休息模式或窗口模式下拉框  | 下拉菜单为暖色纸面风格，不是深灰系统菜单   | 通过  |     |
| V04-MAN-036 | 切换“面板”页         | Panel 相关设置不溢出、不套框      | 通过  |     |
| V04-MAN-037 | 切换“通用”页         | 开机自启、关闭隐藏、重置类操作布局清晰    | 通过  |     |
| V04-MAN-038 | 点击保存            | 保存反馈出现，配置写入成功          | 通过  |     |
| V04-MAN-039 | 点击取消/关闭         | 设置窗口关闭，不影响桌宠窗口         | 通过  |     |

## 7. Wizard UI

| 编号          | 操作步骤       | 预期行为                   | 结果                 | 备注                                                                 |
| ----------- | ---------- | ---------------------- | ------------------ | ------------------------------------------------------------------ |
| V04-MAN-050 | 从设置中重新运行向导 | 向导打开，风格与 Settings 保持一致 | 通过                 |                                                                    |
| V04-MAN-051 | 查看欢迎页      | 文案和布局像桌面小工具配置，不像默认弹窗   | 通过                 |                                                                    |
| V04-MAN-052 | 填写薪资页      | 输入控件清晰可用               | 暂缓，转后续优化 | 多轮修复后用户体感无明显变化；暂不继续在 v0.4 收尾阶段反复打补丁，记录为 `V04-OPT-001` 后续专项优化 |
| V04-MAN-053 | 进入宠物选择页    | 可选择橘猫 v2 / 旧素材回退项      | 通过                 |                                                                    |
| V04-MAN-054 | 完成向导       | 配置保存，回到桌宠模式            | 通过                 |                                                                    |

## 8. 托盘与找回

| 编号          | 操作步骤             | 预期行为                      | 结果  | 备注                             |
| ----------- | ---------------- | ------------------------- | --- | ------------------------------ |
| V04-MAN-060 | 查看 Windows 右下角托盘 | 托盘图标显示为应用图标，不是默认 Godot 图标 | 通过  |                                |
| V04-MAN-061 | 左键点击托盘图标         | 桌宠窗口隐藏/显示切换               | 已修复待复测 | 已修复：托盘恢复显示后会专门重套窗口策略、纯桌宠任务栏策略和点击穿透策略，并在下一帧二次确认 |
| V04-MAN-062 | 右键点击托盘图标         | 出现菜单，可打开设置/关于/退出          | 通过  |                                |
| V04-MAN-063 | 设置关闭时隐藏到托盘       | 关闭窗口后进程仍可从托盘找回            | 通过  |                                |

## 9. 稳定性与日志

| 编号          | 操作步骤            | 预期行为                         | 结果  | 备注  |
| ----------- | --------------- | ---------------------------- | --- | --- |
| V04-MAN-070 | 连续打开/关闭设置 5 次   | 不崩溃，不出现黑块残留                  | 通过  |     |
| V04-MAN-071 | 连续切换 tab 10 次   | 布局稳定，不卡死                     | 通过  |     |
| V04-MAN-072 | 连续单击/双击/右键各 5 次 | 日志记录正常，视觉反馈正常                | 未测试 | 复测：分别对小猫执行单击 5 次、双击 5 次、右键 5 次；确认视觉反馈可见，右键菜单稳定弹出，应用不崩溃 |
| V04-MAN-073 | 查看 debug.log    | 无 parser/runtime 红色错误对应的异常日志 | 未测试 | 复测：运行后执行 `Get-Content "$env:APPDATA\LetsMakeMoney\debug.log" -Tail 80`，确认无 parser/runtime/Invalid call/null instance 等异常 |

## 10. 问题记录

```text
编号：
运行方式：
复现步骤：
期望行为：
实际行为：
日志片段：
截图/录屏：
初步判断：
```
