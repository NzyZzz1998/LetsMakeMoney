# LetsMakeMoney Windows v1.0.8 支持矩阵
## 最终候选口径

本矩阵描述候选 `V10F-20260804-final-81abae36` 的真实验证边界，不继承 v1.0.7 的通过结论。候选 EXE、WebView、窗口参数、品牌资产或 CSS 发生变化后，受影响的真实 Windows 证据必须重新取得。

任何候选身份或相关实现发生变化时，受影响项目立即恢复为“待重新验证”，不得沿用本候选结论。

| 环境 | v1.0.8 状态 | 发布口径 |
| --- | --- | --- |
| Windows 11 x86_64、单显示器、100% DPI | 通过 | 已验证环境 |
| Windows 11 x86_64、单显示器、125% DPI | 通过 | 已验证环境 |
| Windows 11 x86_64、单显示器、150% DPI | 通过 | 已验证环境 |
| Windows 10 x86_64 | 未验证 | 不写入已验证支持声明；仅尽力兼容 |
| Windows 11 多显示器 | 暂不验证 | 不进入 v1.0.8 通过声明 |
| 非 x86_64 Windows | 不支持 | 不生成发布包 |

WebView2 Runtime 是运行前提。缺失、损坏或初始化失败时必须给出可理解的系统提示，不能用浏览器 fallback 冒充正式桌面运行。

## 候选验证结果

- Mini、Workbench、Settings、Wizard 的浅色/深色和三档 DPI：通过。
- TimeField、Combobox、窗口单一表面和隐私竖条：通过。
- 加班迁移、创建/修改/删除、日期联动和月度统计：通过。
- 贴边收起、冷启动恢复和窗口位置：通过。
- 候选 Zip、EXE、版本元数据、README 和哈希一致性：通过。
- Windows 通知区左键/右键、任务栏入口和退出：待项目所有者真实鼠标补证。
