# LetsMakeMoney Windows v1.0.8 手动验收
> 对象：`main@81abae364ad577a394c3c9dcda3a1d1c15e83b99`，候选 `V10F-20260804-final-81abae36`。

## 对象锁定

- [x] 分支、40 位 HEAD 和 clean 构建状态已记录。
- [x] 候选由 `scripts/package_v10f.ps1` 从干净提交生成。
- [x] Zip、EXE、WebView2Loader、README 和 BUILD-INFO 的大小与 SHA256 已记录。
- [x] GUI 只运行全新独立解压目录中的 EXE。
- [x] 用户配置、加班记录、日志和本地状态已备份。

## Windows 11 单显示器矩阵

| ID | 范围 | 结论 | 备注 |
| --- | --- | --- | --- |
| V10F-ACC-001 | 首次启动 | 通过 | 真实空配置 Wizard 三步及 150% DPI 复验通过 |
| V10F-ACC-002 | Mini | 通过 | 拖动、贴边、隐私竖条、展开/收起、冷启动恢复通过 |
| V10F-ACC-003 | Workbench | 通过 | 打开隐藏 Mini，关闭恢复进入前状态 |
| V10F-ACC-004 | 日期调整 | 通过 | 双击调整、应用、取消和自动判断入口通过 |
| V10F-ACC-005 | 加班事务 | 通过 | 8h 创建、7.25h 修改、持久化和删除确认通过 |
| V10F-ACC-006 | 周末联动 | 通过 | 自然周末改工作日时默认 8h，日期与加班同步保存 |
| V10F-ACC-007 | 历史兼容 | 通过 | 自动迁移、兼容导出和 schema 行为测试通过 |
| V10F-ACC-008 | 月度总结 | 通过 | 计划、实际、加班统计及六周整月布局通过 |
| V10F-ACC-009 | 控件 | 通过 | TimeField、Combobox、Escape、取消和确认通过 |
| V10F-ACC-010 | 窗口表面 | 通过 | 三档 DPI 未见双层弧线、阴影裁切或框中框 |
| V10F-ACC-011 | 托盘 | 通过 | 项目所有者使用真实鼠标完成通知区左键隐藏/恢复、右键菜单、任务栏策略与退出；退出后进程消失，日志记录 `tray-exit` 与 `app.exit_requested` |
| V10F-ACC-012 | 错误与诊断 | 通过 | 自动失败回滚测试 + GUI 诊断脱敏、保存/无变化/取消链路 |
| V10F-ACC-013 | 更新与版本 | 通过 | GUI 显示 1.0.8，检查更新为当前最新版 |
| V10F-ACC-014 | 长内容与 DPI | 通过 | 100%、125%、150% 常用窗口无裁切、重叠和整月溢出 |

## 项目所有者补证

项目所有者已在当前锁定候选上使用真实鼠标完成：

1. 单击通知区图标隐藏 Mini，再次单击恢复：通过。
2. 右键通知区图标，确认菜单、设置、今日详情和退出入口：通过。
3. 普通模式恢复后确认任务栏入口策略符合产品合同：通过。
4. 托盘退出后确认 `LetsMakeMoney.exe` 进程消失：通过；日志包含 `event=tray.command detail=id=tray-exit` 和 `event=app.exit_requested detail=source=tray`。

## 支持边界

- Windows 11 x86_64、单显示器、100%/125%/150% DPI：通过。
- Windows 10：未验证，仅尽力兼容。
- 多显示器：暂不验证。
- Release 只允许便携 Zip 与 `SHA256SUMS.txt`；不上传临时证据或其他产物。

## 环境恢复

- [x] 停止全部候选进程。
- [x] 恢复原始 roaming/local 配置与日志。
- [x] 确认开机启动注册表与验收前一致（仍为空）。
- [x] 确认 Windows 显示缩放恢复为 100%。
- [x] 保留唯一候选与脱敏摘要；本机原始证据不进入发布包。
