# LetsMakeMoney Windows v1.0 Stable 发布清单

## 当前状态

**验收通过，可进入发布收口。** M7 Acceptance 已通过；多显示器待补证但经项目所有者批准不阻塞，干净提交候选已生成。

## 事实与合同

- [~] 实施提交、便携 Zip、EXE 和依赖身份已锁定；tag 尚未创建。
- [x] v0.9 Beta 回退 tag、Release 与三组产物哈希已记录。
- [x] v1.0 配置、迁移、窗口、日志和视觉合同已建立。
- [x] 大小周本周类型没有隐式默认值。
- [x] v1.0 正式工程与 v0.9 Godot 基线隔离。

## 开发门禁

- [x] Rust domain、config、platform 测试在 M0-M6 分阶段通过。
- [x] React 合同、流程、可访问性和浏览器视觉门禁在 M1-M4 通过。
- [x] 真实 100%、125%、150% DPI 通过。
- [x] 真实通知区鼠标左键与 Explorer 重启后托盘重注册通过。
- [x] v0.9 配置迁移、失败补偿和官方 Release 包桌面回退通过。
- [x] 源码、运行时和配置零宠物能力检查通过。
- [x] 发布包零宠物能力检查通过。

## 发布包

- [x] 已从干净实现提交生成便携 Zip。
- [x] 独立解压目录首次启动、配置、三档真实 DPI、通知区真实左键与长期运行通过；多显示器待补证但不阻塞。
- [x] Zip、EXE、依赖与许可文件 SHA256 已记录。
- [x] 新候选迷你收入视图、Today、Calendar、Settings 四任务组与无变化保存完成真实 GUI 定向复验。
- [x] 迷你收入视图可从顶部拖动柄移动，位置会持久化并在重启后恢复。
- [x] 包内没有 Godot 运行时、宠物资源、开发缓存或本机路径。
- [x] README、迁移、回退、许可和已知限制与候选身份一致。

## 发布批准

- [x] `FR-001` 至 `FR-012` 核心黄金路径通过；多显示器待补证已按项目所有者决定列为非阻塞项。
- [x] `progress`、`verification`、release notes、changelog 与 current 状态一致。
- [ ] 将 `Protect_main` 必需检查从旧版 `Windows docs and compliance`、`Windows native and Godot verification` 更新为 `Windows v1 verification`；保留 PR、squash、禁止删除和禁止强推规则。
- [ ] 项目所有者批准 Stable 发布。
- [ ] 提交、推送、tag 和 GitHub Release 仅在明确授权后执行。
