# LetsMakeMoney Windows v0.9 Beta 候选检查清单

**当前状态**：`V09-BUG-002` 至 `V09-BUG-008` 均已关闭。锁定候选已完成最终 V09-ACC，结论为“通过 / 可进入发布收口”；本轮尚未执行提交、推送、tag 或 Release。

## 候选身份

- Zip SHA256：`B10FDE2027D4ABC71C41F0F7AC7BDCE3D93AEB8AFAF4058BA1A592B6A75CC1EC`
- EXE SHA256：`E56AB6F045BF6F9E241AB42719BDF00B925754EC3FF0C9083586EB04DECEFC13`
- Native DLL SHA256：`91B1BD23CF48A422AACB66A23B8B09CDE90772039D8D2622E1C703EF03AEB2D4`
- 独立验收不得使用旧 Zip 或 `build/` 目录产物代替。

## 自动门禁

- [x] `project.godot` 版本为 `0.9-beta`。
- [x] v0.9 计薪、配置、窗口、宠物包、动画与集成测试通过。
- [x] v0.8、M4、M5、配置、托盘和历史兼容回归通过。
- [x] 候选 Zip、EXE、DLL 的路径、大小与 SHA256 已记录。
- [x] 包内 README、发布说明、许可、manifest 和 checksums 完整。
- [x] 从独立解压目录启动烟测通过。
- [x] 文档状态、UTF-8、乱码、本地链接与 `git diff --check` 通过。

## 独立验收

- [x] 新候选包内 Classic Pro 与多多均成功加载，日志不得出现 `PetManager.package rejected`。
- [x] 包验证器检查运行时宠物加载结果，失败时返回非零退出码。
- [x] `V09-BUG-002` 修复后，真实 Pet 场景产生动作请求、开始、完成和基础状态恢复日志。
- [x] `V09-BUG-003` 修复后，50%/58%/100%/150% 下 Panel 外壳、内容、进度条和文字保持同一缩放合同且无溢出。
- [x] `V09-BUG-004` Classic 单击动作结束并恢复基础状态，活动令牌正常释放。
- [x] `V09-BUG-005` 今日详情、Settings 和 Wizard 使用一致的作息时间与分钟级午休时长显示。
- [x] 100% DPI 下 Settings 五页、今日详情、Wizard 四步与取消、右键菜单以及 Popup/Modal 穿透恢复完成真实 GUI 续测。
- [x] `V09-BUG-006` 修复后，保存成功、真正无变化和保存失败三条反馈均完成真实 GUI 复验。
- [x] `V09-BUG-007` 修复后，关于窗口图标、版本、说明和配置路径完整可访问且无裁切。
- [x] `V09-BUG-008` 多多包 manifest 哈希与实际运行时载荷一致，完整回归和包体验证通过。
- [x] 最终候选完成 Panel、今日详情、Settings、Wizard、菜单、Classic/多多切换与单击、Popup/Modal 穿透和日志回归。
- [x] 普通/纯桌宠 native 托盘消息各 10 轮、宠物包损坏回退、v0.8 行为基线和 60 秒稳定冒烟通过。
- [ ] 100%/125%/150% DPI 下 Panel、Settings、Wizard、菜单与今日详情无裁切。
- [ ] Classic 当前三基础状态和通用回退不回退；多多 S5.5 的三状态单击、跑动收势和午休事件动作可辨认且完整恢复。
- [ ] 长按进入跑步准备，左右拖动方向正确，释放收势后恢复最新基础状态且不补发单击。
- [x] 自动与定向门禁确认双击不触发独立产品动作；状态感知单击、指针观察和动态命中区合同保持稳定。
- [ ] 普通/纯桌宠托盘显隐、任务栏入口和点击穿透符合 v0.8 行为。
- [x] Classic 一键回滚、旧用户选择保持和损坏包自动回退合同通过；损坏包真实桌面观感按暂不验证保留。
- [ ] 连续运行 2 小时无明显动画卡死、命中漂移或窗口策略失效。
- [x] V09-ACC 在新候选上给出最终结论：“通过，可进入发布收口”。

任一候选身份不一致、计薪错误、配置污染、宠物不可见、核心交互回退或点击穿透失效均阻塞发布。

当前无发布阻塞。真实通知区鼠标/任务栏、500ms 长按跑动和两套宠物完整三状态观感保留为待人工补证；真实 125%/150% DPI、受控损坏包桌面回退观感和两小时稳定运行保留为暂不验证。以上内容不得改写为通过，但按已确认验收规则不阻塞 v0.9 Beta 发布收口。

## 发布执行

- [x] 项目所有者选择重新打包最终文档快照；新 Zip 哈希、包体验证和新解压冒烟均已完成，EXE 与 Native DLL 身份未变化。
- [ ] 提交锁定候选对应的修复与发布收口文档。
- [ ] 推送目标分支。
- [ ] 创建并推送 annotated tag `v0.9-beta`。
- [ ] 创建 GitHub Pre-release，仅上传锁定便携 Zip 和 `SHA256SUMS.txt`。
- [ ] 发布后核对远端提交、tag、附件和 SHA256。
