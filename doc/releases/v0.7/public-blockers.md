# v0.7 公开阻塞清单

## P0

当前无未关闭 P0：两种专业 secret scanner 对当前树和完整历史均为 0 命中；没有待轮换凭据或无权公开第三方资产证据。

## 已签核、待执行的历史重写阻塞

| ID | 发现 | 当前树 | Git 历史 | 解决路径 |
|---|---|---|---|---|
| PUB-001 | `temp/` 43 个路径及 1 个 Zip | 已跟踪 | 自 `466de035...` 可访问 | B3 清当前树；历史重写移除 |
| PUB-002 | `experiments/ai_cat_assets/` 20 个路径 | 已跟踪 | 自 `8a598323...` 可访问 | B3 清当前树；历史重写移除 |
| PUB-003 | ComfyUI 文档/脚本 7 个路径 | 已跟踪 | 自 `b2b67f08...` 可访问 | B3 清当前树；历史重写移除 |
| PUB-004 | 38 个历史路径含 Windows 绝对路径 | 部分仍活跃 | 23 个提交受影响 | B1/B3/E1 清当前树；历史使用精确替换表清洗 |
| PUB-005 | Git 作者个人邮箱 | 不属于工作树 | 完整历史可见 | 使用仓库外私有映射统一为 GitHub noreply 身份 |

项目所有者已选择方案 3。以上阻塞在 `git-history-rewrite-plan.md` 完成并复验前保持开放。

## 非阻塞观察

- 历史 7 个大于 1 MiB 的 blob 均小于 5 MiB，没有大型构建产物。
- 5 个已删除路径是历史验证/测试噪音，没有秘密或私有配置。
- 本地 `.manual-test/`、`.tmp_acceptance/`、发布展开目录和 build 缓存合计体积较大，但均未被 Git 跟踪。
