# v0.7 公开排除清单

| 路径/类型 | 是否进入仓库 | 是否进入发布包 | 原因 | 当前处置 | 后续责任 |
|---|---|---|---|---|---|
| `.manual-test/` | 否 | 否 | 私有手工验收备份、配置、日志 | `.gitignore` 排除；不删除 | A0/A3 |
| `.tmp_acceptance/` | 否 | 否 | 私有截图、日志、配置、解压目录 | `.gitignore` 排除；不删除 | A0/A3 |
| `.tmp_*`、`_lmm_verify/` | 否 | 否 | 临时验证、缓存和中间产物 | `.gitignore` 排除 | A0/B2 |
| `releases/v*/` 本地版本目录 | 否 | 否（源码仓库） | 已验收 Zip、EXE/DLL 与展开目录由 GitHub Release 承载 | 精确 ignore；保留根发布说明 Markdown | A0/C/E4 |
| `%APPDATA%/LetsMakeMoney` 的配置、日志、备份 | 否 | 否 | 用户私有运行数据 | 不得复制进仓库或包 | A0/A3 |
| ComfyUI 本机程序、模型、工作流缓存和下载包 | 否 | 否 | 实验能力不产品化，体积与第三方许可复杂 | 本机程序/模型未跟踪；历史含 7 个相关文档/脚本，待所有者签核披露 | A3/B3 |
| 未确认来源的临时素材、生成中间图、模型输出 | 否 | 否 | 无法证明公开与再分发授权 | A1/A3 签核前阻塞 | A1/A3 |
| 证书、私钥、密码、token、`.env` | 否 | 否 | 高敏感凭据 | ignore + 当前树/历史扫描；发现即阻塞 | A0/A3/C2 |
| `native/windows/godot-cpp/` 本地 checkout | 否 | 否 | 离线依赖缓存；未来由固定 commit bootstrap 获取 | ignore；不删除 | B1 |
| `build/`、Godot import/cache、native object/lib | 否 | 否 | 可再生构建缓存 | ignore | B1/B2 |
| installer/update 中间缓存 | 否 | 否 | 可再生且可能含签名临时文件 | ignore | C1-C4 |
| 本机绝对路径、用户名、私有测试目录引用 | 否 | 否 | 隐私和不可复现 | 活跃文档/脚本必须替换；历史由 A3 判断 | A0/A3/E1 |

## 重要说明

- `.gitignore` 不会移除已跟踪的 `temp/`、`experiments/` 或历史提交内容。
- A0 不删除任何文件。A1/A2/A3 形成证据后，才可提出迁移、清理或历史风险处置方案。
- 扫描结果只显示路径、规则和风险，不在文档中复制秘密值。

## A3 历史结论

- 专业秘密扫描未发现真实凭据，P0 为 0。
- 完整历史包含 43 个 `temp/` 路径、20 个实验素材路径、7 个 ComfyUI 文档/脚本路径，以及若干本机绝对路径。
- 上述素材权属没有未知项，但仍属于项目所有者明确排除的公开内容。B3 只能清理当前树，不能改变历史可访问性。
- `history-owner-signoff.md` 已签核方案 3；历史重写与复验完成前仓库继续保持私有，不得把这些路径加入白名单来规避门禁。
