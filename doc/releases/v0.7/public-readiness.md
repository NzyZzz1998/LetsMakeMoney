# v0.7 公开准备状态

**结论**：当前不适合公开。A0/A1/A2/A3 与远端历史清洗已完成；后续工程、分发、社区治理和独立公开验收门禁仍未完成。

## A0 已冻结规则

1. 当前 HEAD、脏工作区、v0.6 验收产物、v0.7 开发候选和未来公开候选必须分别记录。
2. `.gitignore` 只防止新增本地文件误提交，不能处理已跟踪文件或 Git 历史。
3. 当前树检查器默认只读、失败返回非零、只报告路径/规则/风险，不输出秘密值。
4. 完整历史、删除文件历史、作者信息和历史资产由 V07-A3 审计。
5. 任何真实凭据或无权公开资产未关闭前，仓库保持私有。

## 公开门禁

| 类别 | 当前状态 | 公开前要求 |
|---|---|---|
| 当前事实 | 已完成 | 当前入口、清单、脚本和 Git 事实一致 |
| 代码许可 | 已完成 | 根 `LICENSE` 为 MIT，版权口径为 NzyZzz1998（2026） |
| 素材许可 | 已完成 | 受限许可、逐类来源、清单和所有者签核已建立 |
| 第三方依赖 | A2 结构完成 | 人机清单、原文、notices 和包内规则已建立；固定 bootstrap/安装器/CI 身份待 B1/C1/E3 |
| 当前树安全 | P0 已关闭，排除项未清理 | B3 清理已跟踪排除内容后重新扫描 |
| 完整历史安全 | 已完成 | 远端 heads/tags 已替换；fresh clone 的双扫描、路径/邮箱归零和回归通过 |
| 可复现构建 | 未完成 | 干净环境能构建、测试、运行和打包 |
| 产品回归 | 未完成 | v0.6 核心体验和 Main/native 状态矩阵通过 |
| 分发 | 未完成 | Zip/安装器/更新分别通过门禁 |
| 社区治理 | 未完成 | 双语入口、贡献、安全和 GitHub 设置可用 |
| 独立验收 | 未完成 | `/acceptance` 通过且项目所有者批准公开 |

## 当前已知转交

- `temp/` 和 `experiments/` 含已跟踪素材/实验文件，转交 A1/A2/A3 审核，不因新增 ignore 自动消失。
- v0.6 发布目录和私有验收证据仅保留在本机，排除出源码候选。
- A3 未发现真实秘密或无权公开第三方资产，但完整历史仍包含明确排除的自有实验内容和本机路径；所有者已签核方案 3，重写与复验完成前不得公开。

## A1 验证结果

- 公开候选视觉覆盖通过：橘猫 v1/v2、占位猫、Logo/图标和原型视觉均有许可归属。
- `_review`、`experiments/ai_cat_assets` 和 `temp/` 标记为 `excluded_private`，不获得公开授权。
- `scripts/check_asset_licenses.ps1` 通过；正向夹具通过，未登记视觉夹具按预期失败。
- 所有者签核记录：`doc/releases/v0.7/asset-owner-attestation.md`。
- 排除项仍存在于已跟踪树时，A0 公开候选检查继续失败，必须由 A3 处置。

## A2 验证结果

- 当前源码合规检查通过；13 份许可证原文均有固定 SHA256。
- 受控包正常路径通过；缺依赖条目、缺许可原文、未知 DLL、未知字体和 notices 版本不一致均按预期失败。
- v0.6 展开包只读审计失败：缺少 v0.7 定义的 `LICENSES/` 目录和运行时许可证原文。旧包保持原样，不是 v0.7 公开候选。
- Inno Setup 尚未安装/选版，只阻塞未来安装器；GitHub Actions 尚未选择，只阻塞后续 CI 门禁。
- 当前 `temp/` 和历史问题保持公开阻塞，未被加入合规白名单。

## A3 验证结果

- Gitleaks 8.30.1 和 TruffleHog 3.95.9 扫描重写后的当前候选快照及全部可达提交，真实秘密命中均为 0。
- Git 对象审计覆盖 844 个对象、551 个 blob；历史中没有 EXE、DLL、私钥、用户配置或运行日志。
- 历史包含 43 个 `temp/` 路径、20 个实验素材路径、7 个 ComfyUI 路径以及本机绝对路径。这些不是秘密或无权资产，但与既定公开排除政策冲突。
- `.manual-test/`、`.tmp_acceptance/`、本地发布展开目录和 `build/` 均未进入 Git 历史。
- P0 已全部关闭；方案 3 已完成远端重写和复扫。GitHub Release 仍使用原 tag 名称，附件页面关联需在登录态页面做一次只读人工确认。
- 详细证据见 `git-history-audit.md`、`public-blockers.md` 和 `history-owner-signoff.md`。
- 本地独立镜像演练已实现目标路径、绝对路径和旧邮箱残留均为 0，双扫描与 v0.4-v0.6 回归通过；证据见 `git-history-rewrite-dry-run.md`。GitHub 远端仍保持原历史。
