# LetsMakeMoney v0.7 Beta 人工验证

**状态**：Computer Use 已完成可覆盖部分；其余待人工补证

**验收身份**：`main` / `9e76435973b8e12b3fe10199660fcb257aa968a6`

**本地证据**：`.tmp_acceptance/v0.7-final-20260711-220351/evidence/`

## 验收对象

仅使用 `releases/v0.7/` 中新解压的候选包和当次生成的安装器，不使用 `build/` 旧产物替代。记录 Zip、EXE、DLL、安装器 SHA256、分支和 HEAD。

## 必须人工补证

1. **待人工补证**：Windows 通知区真实左键隐藏/恢复；普通模式恢复后任务栏入口存在，纯桌宠恢复后任务栏入口不存在。Computer Use 无稳定通知区入口；纯桌宠显示态已由日志和应用列表证明任务栏入口隐藏。
2. **部分通过**：100% DPI 的 Panel、菜单、Settings、Wizard 和主要弹窗通过；当前设备只有一个显示器。125%、150%、200% DPI 与双显示器跨屏、记忆、断开回落待人工补证。
3. Settings 更新页：稳定/测试通道、检查中、无更新、发现更新、离线、失败、取消与重试反馈。
4. **部分通过**：GUI 取消、正常安装、覆盖/修复、默认保留数据卸载通过；显式勾选删除数据、安装日志可读性仍待人工补证。
5. **未通过公开门禁**：安装器为 `NotSigned`，运行时未出现可信发布者证据。不得作为 Release 附件。
6. 更新：下载完成后再次确认；取消不退出；安装前生成配置备份；安装版与便携版共享 APPDATA 提示可见；失败可返回 GitHub Release。
7. 真实 Windows 登录后的开机自启继续单独记录，自动脚本不能替代真实登录证据。

每项只使用：通过、部分通过、失败、待验证。截图和日志证据放在本地验收目录，不提交隐私内容。

## 本轮发现的发布阻塞

- `V07-BUG-001`：制造 `config.json.tmp` 为目录后保存，输入保留、旧配置哈希不变、失败日志完整，但 Settings 页面在 0.35 秒和 1.25 秒截图中均未显示可见失败反馈。修复后必须用同一故障注入复验。

## 人工记录区

| ID | 环境/操作 | 结果 | 证据 | 结论 |
|---|---|---|---|---|
| V07-MAN-001 | 通知区真实左键普通/纯桌宠各 3 轮 |  | 截图 + `debug.log` | 待验证 |
| V07-MAN-002 | 125% / 150% / 200% DPI |  | 各页面截图 | 待验证 |
| V07-MAN-003 | 双显示器跨屏、重启、断开回落 |  | 截图 + 配置坐标 | 待验证 |
| V07-MAN-004 | 卸载勾选删除数据并二次确认 |  | 卸载截图 + APPDATA 结果 | 待验证 |
| V07-MAN-005 | 真实 Authenticode 与 SmartScreen |  | 签名属性 + SmartScreen 截图 | 未通过（当前无签名） |
| V07-MAN-006 | 干净 Windows 用户/VM 按双语 README 首次使用 |  | 命令记录 + 产物哈希 | 待验证 |
