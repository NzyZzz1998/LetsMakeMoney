# LetsMakeMoney Windows v1.1.0 手动验收

所有步骤只能运行从当前 Zip 新解压目录中的 `LetsMakeMoney.exe`。本轮测试候选为 `V110-20260813T081246Z-f5ae4ac3-clean`，Zip SHA256 为 `E79D3716400D74E9E2F5419700B97630F273AA67761982081AED07E8C87C6EB7`。开始前备份应用数据目录，结束后恢复。

## 身份与启动

- [x] 核对 Zip、EXE、WebView2Loader.dll 大小与 SHA256。
- [x] 首次启动默认显示 Mini，不自动启用 Classic。
- [x] 设置切换 Classic 后 Mini 消失；重启后只恢复 Classic。
- [x] 切回 Mini 后 Classic 被销毁或隐藏，不能同时出现。

## 状态与交互

- [x] working、awake_rest、sleeping 均可辨认并稳定循环。
- [x] 每种基础状态单击 10 次，使用各自反馈并恢复正确状态。
- [x] 长按 500ms 后拖拽 5 次，覆盖左、右和快速反向。
- [x] 释放后完整播放 run_stop；拖拽不误判为单击。
- [x] 拖拽期间窗口失焦/截图会取消活跃输入、完整收势，随后仍可正常单击和再次拖拽。
- [x] 可见像素可交互，透明像素穿透；自动 hit mask 探针与真实 GUI 点击一致。
- [x] 右键菜单、Settings 和 Modal 打开期间输入锁定，关闭后恢复。

可继承历史证据：sleeping / working / awake_rest 三基础循环及各自状态反馈连续 10 次通过；旧候选上的 5 次 500ms 长按拖拽、左右反向和完整收势通过；右键菜单锁定/释放成对通过。当前 Zip 已完成截图中断恢复及 Settings/Modal 输入锁定的本机连续闭环。

当前干净候选已通过帧级重组缩小 idle/基础循环与跑动姿态的体量差异。2026-08-17，项目所有者在本机直接输入环境完成抓取连续性、拖拽跟手、快速反向、截图中断恢复与后续再次交互，`V110-UX-001` 与 `V110-UX-002` 人工结论均为通过。

- [x] 本机环境从猫头长按进入跑动后，指针仍落在可见角色主体上。
- [x] 本机环境快速左右折返无明显滞后、积压追赶或方向延迟。
- [x] 本机环境截图/失焦中断后可再次单击和拖拽，无陈旧位移继续执行。

## 窗口与托盘

- [x] 打开 Workbench 时只隐藏当前陪伴；关闭后恢复进入前模式。
- [x] Workbench 打开期间切换陪伴模式无死锁、无双窗口。
- [x] 托盘左键隐藏/恢复、右键菜单和退出符合任务栏策略。
- [x] 普通隐藏、找回、重启和异常窗口关闭不改变用户选择。

当前候选已完成真实通知区左键隐藏/恢复、右键菜单命令逐项执行、退出及任务栏策略闭环。

## DPI 与视觉

- [x] 100% DPI：单个动作内边缘清晰，无蓝边、裁切或脚底漂移。
- [x] 125% DPI：窗口、动画、命中和拖拽正确。
- [x] 150% DPI：窗口、动画、命中和拖拽正确。
- [x] 每次切换 DPI 后确认位置可达；最终恢复用户原 DPI。

当前 Zip 的 256×208 逻辑画布已在 100%、125% 与 150% DPI 下完成人工复核，覆盖可见性、裁切、透明命中和完整 500ms 拖拽链路。

## 2026-08-17 本机补证

- 运行目录：`E:\codex\.tmp-lmm-v110-manual-20260817-105056`。
- EXE SHA256：`0D02CC9AA628FD7FC66EADFB518E36BFE023689F82581C52F258B75A0694384B`。
- 项目所有者按本文件对应的九组手动步骤完成模式互斥、状态点击、长按拖拽、快速反向、截图中断、Workbench 协同、Settings/Modal 锁定、托盘命令/退出和三档 DPI 验证，并明确反馈“全部通过”。
- 本轮结论只适用于上述锁定候选；正式发布仍需从最终文档提交后的干净源重新构建并更新哈希。

## 故障与长期运行

- [ ] manifest 损坏时回落 Mini，收入主线可用且日志说明原因。
- [ ] 图集缺失或哈希错误时回落 Mini，配置不被永久污染。
- [x] 连续观看 30 分钟，无机械高频重复或明显干扰。
- [x] 连续运行两小时，无动画卡死、命中漂移、重复 timer 或异常资源增长。

## 环境恢复

- [x] 停止全部 LetsMakeMoney 进程。
- [x] 配置、加班记录与日志逐文件 SHA256 与验收前备份一致。
- [x] Windows 显示缩放恢复为验收前的 100%。

无法由自动化可靠完成的通知区、DPI 和主观动画观感必须保留真实人工结论，不得用自动门禁替代。
