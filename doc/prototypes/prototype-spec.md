# LetsMakeMoney Prototype Spec

**版本**: v0.1 Beta / v0.2 Beta / v0.3 Beta 原型说明  
**日期**: 2026-07-02  
**原型入口**: `doc/prototypes/index.html`

---

## 1. 原型目标

本原型用于补齐 LetsMakeMoney 在 v0.1 到 v0.3 期间缺失的界面设计表达。它不是最终视觉稿，也不是 Godot 场景实现文件，而是一个用于产品 review、工程对照和后续 UI 实现讨论的中保真交互原型。

原型重点回答这些问题：

- v0.1 调试窗口版、v0.2 紧凑桌宠候选、v0.3 真桌宠修复版的界面关系是什么。
- 桌宠主窗口、薪资 Panel、右键菜单、设置窗口、首次启动向导、系统托盘、Debug 模式、纯桌宠模式分别长什么样。
- v0.3 的真托盘、透明无边框、点击穿透、关闭隐藏到托盘和纯桌宠模式如何被用户理解。
- 每个界面对应哪些配置字段、验收点和降级条件。

---

## 2. 交付形式

```text
doc/prototypes/
├── index.html
└── prototype-spec.md
```

`index.html` 是单文件交互式 HTML 原型，直接用浏览器打开即可查看。它引用项目已有素材：

- 橘猫素材：`../../assets/pets/cat_orange_v1/frames/...`
- App icon：`../../icons/app_icon.png`

没有复制素材，避免维护多份资源。

---

## 3. 原型范围

### 3.1 已覆盖界面

| 原型屏 | 版本来源 | 设计目的 |
|--------|----------|----------|
| 产品总览 | v0.1 / v0.2 / v0.3 | 展示从调试窗口版到真桌宠修复版的演进 |
| 桌宠主窗口 | v0.2 / v0.3 | 展示小猫、Panel、透明窗口边界、点击穿透区域 |
| 薪资 Panel | v0.1 / v0.2 / v0.3 | 展示折叠金额、展开详情、工作进度和状态 |
| 右键菜单 | v0.1 / v0.2 / v0.3 | 展示设置、向导、角色、窗口模式、关于、退出 |
| 设置窗口 | v0.1 / v0.2 / v0.3 | 展示五个设置页和 v0.3 模式类控件修正 |
| 首次启动向导 | v0.1 / v0.2 | 展示欢迎、薪资、角色、完成四步流程 |
| 系统托盘 | v0.3 | 展示托盘图标、菜单、显示/隐藏、退出 |
| Debug 模式 | v0.1 / v0.2 / v0.3 | 展示 900x500 调试窗口、DebugInputArea、DebugStatus |
| 纯桌宠 / 穿透状态 | v0.3 | 展示任务栏/Alt+Tab 隐藏条件、托盘健康门禁和失败回退 |

### 3.2 不覆盖内容

- 不做最终高保真视觉稿。
- 不模拟真实 Win32 托盘 API。
- 不模拟真实窗口拖拽。
- 不实现薪资计算逻辑。
- 不做橘猫动画打磨；动画打磨进入 v0.4。
- 不做 macOS / Linux 原生能力原型。

---

## 4. 视觉方向

本次原型采用偏工具软件的中保真风格：

- 以浅灰、白色、深色文字和状态色为主。
- 小猫保留真实素材占位，但不把整体做成游戏/玩具风。
- 设置、Debug、状态说明和降级逻辑优先清晰。
- 所有界面带右侧开发注释栏，便于对照 PRD、实施计划和验收标准。

---

## 5. 交互说明

`index.html` 支持轻交互：

- 左侧导航切换原型屏。
- “切换 Panel 展开”按钮可在折叠/展开 Panel 间切换。
- “切换设置 Tab”按钮可轮换设置页。
- 设置窗口的 Tab 可点击切换。
- 右侧注释栏会随当前屏更新版本来源、配置字段、验收点和降级风险。

这些交互用于 review 产品结构，不代表最终 Godot 交互实现。

---

## 6. 与 PRD / Plan / Progress 对应关系

### 6.1 v0.1

| PRD/Plan 内容 | 原型对应 |
|---------------|----------|
| 普通调试窗口版产品雏形 | 产品总览、Debug 模式 |
| 薪资 Panel | 薪资 Panel |
| 设置对话框 | 设置窗口 |
| 首次启动向导 | 首次启动向导 |
| 右键菜单降级托盘 | 右键菜单 |

### 6.2 v0.2

| PRD/Plan 内容 | 原型对应 |
|---------------|----------|
| 紧凑桌宠窗口 | 桌宠主窗口 |
| 橘猫素材接入 | 桌宠主窗口、Panel、向导、Debug |
| 设置通用项、自启动、重置位置、恢复默认 | 设置窗口 |
| 托盘、透明、穿透安全暂缓 | 产品总览、桌宠主窗口注释 |
| Debug/普通模式拆分 | Debug 模式、桌宠主窗口 |

### 6.3 v0.3

| PRD/Plan 内容 | 原型对应 |
|---------------|----------|
| GDExtension / native bridge | 右侧注释栏、系统托盘、纯桌宠状态 |
| 真系统托盘 | 系统托盘 |
| 透明无边框窗口 | 桌宠主窗口 |
| 点击穿透 | 桌宠主窗口、纯桌宠 / 穿透状态 |
| 关闭隐藏到托盘 | 系统托盘、纯桌宠 / 穿透状态 |
| 可找回优先的纯桌宠模式 | 纯桌宠 / 穿透状态 |
| 休息模式、窗口模式控件优化 | 设置窗口 |
| 原生能力状态提示 | 设置窗口 |

---

## 7. 关键设计决策

### 7.1 默认仍保留可找回入口

v0.3 虽然要实现纯桌宠模式，但原型明确表达：默认状态仍保留任务栏 / Alt+Tab。只有托盘健康检查通过后，用户才能手动开启纯桌宠模式。

### 7.2 透明空白区域不是交互区

桌宠主窗口和纯桌宠状态都用虚线标注透明空白区域。该区域在 v0.3 中应尽量穿透到桌面或下层窗口。只有小猫、Panel、菜单和设置窗口属于交互区域。

### 7.3 右键菜单是重要降级入口

即使 v0.3 实现真系统托盘，角色右键菜单仍保留“设置 / 关于 / 退出”等入口。托盘不可用时，右键菜单是用户找回和退出应用的重要降级路径。

### 7.4 设置窗口偏工具化

设置窗口不追求强装饰。它承担配置、诊断、原生能力状态反馈和恢复操作，因此原型中强调：

- 明确选择控件。
- 原生能力状态。
- 保存失败不显示假成功。
- 恢复默认不清薪资配置。

---

## 8. 后续使用方式

建议在实现 v0.3 时按以下方式使用原型：

1. 开发 V03-M1 前，用产品总览和右侧注释确认原生能力范围。
2. 开发 V03-M2 前，对照系统托盘屏确认托盘菜单和找回路径。
3. 开发 V03-M3 / V03-M4 前，对照桌宠主窗口和纯桌宠状态确认透明、穿透、交互区域。
4. 开发 V03-M5 前，对照纯桌宠状态确认“可找回优先”。
5. 开发 V03-M6 前，对照设置窗口确认 OptionButton、状态提示和恢复默认范围。
6. 手动验证前，对照原型和 `doc/verification/v0.3.md` 检查用户可见体验是否一致。

---

## 9. 验收标准

原型自身的验收标准：

- `doc/prototypes/index.html` 可以在浏览器打开。
- 左侧导航可以切换所有原型屏。
- Panel 展开/折叠交互可用。
- 设置 Tab 切换可用。
- 每个原型屏都有版本来源、配置字段、验收点和降级风险说明。
- 原型引用当前橘猫素材和 app icon，不使用缺失资源。
- 原型说明文档能解释每个界面与 PRD / implementation-plan / progress 的对应关系。
